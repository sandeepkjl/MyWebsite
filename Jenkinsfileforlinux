pipeline {
    agent any



stages{

		stage('Install package'){
            
            steps {
                sh 'npm install'              
            }
        }
        
        stage('Build'){
            
            steps {
				
                sh 'ng build'              
            }
        }

        
}
}
